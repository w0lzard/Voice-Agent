[build]
builder = "dockerfile"
dockerfilePath = "services/config/Dockerfile"

[deploy]
startCommand = "uvicorn services.config.main:app --host 0.0.0.0 --port ${PORT:-8002}"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
